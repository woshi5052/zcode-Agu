# A股量化 v3.0 修复报告 — 核心代码审查修复完成

> 修复时间：2026-08-08 | 修复范围：桌面 22 文件夹（基于 11 文件夹代码审查结果）
> 状态：**P0×2 已修复并验证，P1×3 已修复，可运行（合成数据冒烟测试通过）**

---

## 一、修复总览

| 问题 | 级别 | 状态 | 修复文件 |
|------|------|------|----------|
| SellEngine 被 import 但从未调用 | P0-A | ✅ 已修复 | backtest/engine.py |
| 大盘过滤在回测中未生效 | P0-B | ✅ 已修复 | backtest/engine.py |
| 股票池 glob[:20] 非显式 | P1-1 | ✅ 已修复 | run_wf.py / validation/walkforward.py / data/universe.py |
| 净值只在平仓日复利，回撤低估 | P1-2 | ✅ 已修复 | backtest/engine.py |
| 止损参考价与成交价错位 | P1-3 | ✅ 已修复 | backtest/engine.py |

**另发现并修复隐藏 bug×2**：
- Supertrend 实现错误（浮点相等比较导致 direction 频繁翻转）→ strategies/indicators.py
- 名义资金 bug（cash=1.0 归一化净值 vs 绝对价格 → 永远买不进）→ backtest/engine.py

---

## 二、P0-A 修复：SellEngine 真正接入

**原问题**：engine.py L10-14 import 了 sell_evaluate，但 run() 从未调用，L193-215 内联重写简化卖出。

**修复**：
1. 删除内联简化卖出逻辑
2. 收盘后评估持仓时构造 `Position` / `Bar`（atr/supertrend/ma20/rsi/volume_ratio/day_change/recent_3d_high 等全字段）传入 `sell_evaluate(pos, bar, regime)`
3. 按返回 `SellSignal.exec_time` 执行：`NEXT_OPEN` 次日开盘成交 / `NEXT_1450` 次日14:50近似收盘成交
4. 卖出后按信号更新止损线；止损类信号触发冷却期（5天禁重买）

**验证**：`tests/test_parity.py::test_sell_engine_invoked` PASS（源码检查 import + 调用存在、无内联简化残留）

## 三、P0-B 修复：大盘过滤逐日生效

**原问题**：index_df 收了不用、can_open_position 从不调用、slots 硬编码 3。

**修复**：
1. `run()` 内每个交易日用 `index_df.loc[:date]` 切片 → `set_index_data()` 逐日更新 regime
2. 生成买单前检查 `can_open_position()`
3. `slots = get_max_positions() - len(positions)`，删除硬编码

**验证**：`test_regime_wired` PASS；冒烟测试确认 regime 随行情切换（bull/sideways/bear）

## 四、P1 修复

- **P1-1 股票池**：新增 `data/universe.py`（显式 universe + 退市名单 + point-in-time 过滤），run_wf.py 改用 `get_universe()`，删除 `glob[:20]`
- **P1-2 净值按日估值**：新增 `INITIAL_CAPITAL=10000` 名义资金，每日 `total_value = cash + Σ(持仓×当日收盘)` 再归一化 → 回撤包含持仓浮亏
- **P1-3 止损基于成交价**：买入成交后用 `fill.price`（实际成交价）重算初始止损

## 五、补充模块（11 文件夹缺失，本工程补齐）

| 文件 | 说明 |
|------|------|
| strategies/indicators.py | 纯 pandas 指标库（MA/RSI/MACD/ATR/Supertrend），无 ta 依赖 |
| execution/broker.py | 虚拟券商：涨停拒买/跌停拒卖/停牌拒单/T+1 |
| execution/cost.py | 成本模型：佣金万2.5 + 印花税万5 + 滑点万5 |
| data/calendar.py | 交易日历 2019-2027 |
| data/adjust.py | get_price 唯一数据门面（前复权） |
| data/universe.py | point-in-time 股票池 |
| tests/test_parity.py | 对拍测试（4 项全部 PASS） |

## 六、验证结果（合成数据冒烟测试）

```
回测: 2022-06-01 → 2023-12-12 | 3 支合成股 | 单票仓位 15%
交易: 2 笔 | 年化 +9.1% | 回撤 -1.25% | 净值曲线 400 点（按日估值）
测试: test_sell_engine_invoked / test_regime_wired / test_sell_engine_direct / test_backtest_runs = ALL PASS
```

> 注：合成数据仅用于验证链路正确性，**不代表真实绩效**。真实数据重跑需先填充 `data/cache/` 的 A 股 K 线缓存（get_price 门面已就绪）。

## 七、遗留事项（给大模型）

1. **真实数据重跑**：填充 data/cache 后跑 `python run_wf.py`，重新上报样本外 PF/回撤（预期回撤会比之前 -40.6% 更大，因已按日估值）
2. **训练段调参钩子**：walkforward.py 中 `params = base_params.copy()` 处预留了 TODO，可接网格搜索
3. **参数档位对比**：single_position_pct 0.15/0.12/0.10 三档重跑，找回撤≤25% 的档位

## 八、目录结构

```
22/
├── config/params.json          # 参数（trailing=3.0/冷却5天/仓位0.15）
├── data/                       # adjust(门面)/calendar/universe
├── strategies/                 # indicators/trend_engine
├── risk/                       # sell_engine.py（完整四层+评分卡）
├── execution/                  # broker/cost
├── backtest/                   # engine(修复版)/metrics
├── validation/                 # walkforward.py
├── tests/                      # test_parity.py（4项PASS）
└── run_wf.py                   # Walk-Forward 入口
```

---

> ⚠️ 以上内容由 AI 基于用户提供的代码审查整理生成，仅供参考，不构成任何投资建议或个股推荐。投资有风险，决策需谨慎。
