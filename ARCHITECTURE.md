# A-Share Quant Platform v2.0 — 架构文档

## 系统全景

```
                     GitHub Actions (调度层)
                     Cron: 交易日 07:30 UTC
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                      runner.py (主入口)                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 数据获取  │→│ A股过滤   │→│ 策略引擎  │→│ 飞书推送  │    │
│  │ AKShare  │  │ 5层过滤   │  │ v2.0     │  │ Bot API  │    │
│  │ 新浪接口  │  │          │  │ 移动止损  │  │          │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│       │             │              │              │          │
│       ▼             ▼              ▼              ▼          │
│  300支日K线     ST/停牌/       Supertrend     飞书「大乔」   │
│  250天回溯      涨跌停/         +ATR+RSI       群卡片消息     │
│                 流动性/         +移动止损                     │
│                 次新股          评分→Top5                     │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 预测追踪  │  │ AI增强   │  │ 回测引擎  │                  │
│  │ 模拟盘    │  │ ModelScope│ │ T+1执行   │                  │
│  │ 胜率统计  │  │ (可选)    │  │ 绩效报告  │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
└──────────────────────────────────────────────────────────────┘
```

## 技术栈

| 层 | 技术 | 说明 |
|------|------|------|
| 调度 | GitHub Actions | 免费，Cron定时，手动触发 |
| 数据 | AKShare (新浪) | 免费开源，日K线+实时行情 |
| 语言 | Python 3.11 | 纯Python，无编译依赖 |
| 指标 | 自研 + ta库 | MA/RSI/MACD/KDJ/ATR/Supertrend |
| AI | ModelScope API | 中文情感分析(可选) |
| 通知 | 飞书 Bot API | App ID+Secret → Token → 群消息 |
| 可视化 | Streamlit + Plotly | 面板：胜率曲线/盈亏分布 |
| 托管 | GitHub + ModelScope | 代码+模型双托管 |

## 目录结构

```
ashare-quant/
├── .github/workflows/daily_update.yml  # Actions调度
├── config/
│   ├── settings.py                     # 全局配置(凭据/环境)
│   └── params.json                     # 策略参数(可调优)
├── data/
│   └── akshare_fetcher.py             # 数据获取+缓存
├── strategies/
│   ├── indicators.py                   # 6个技术指标
│   ├── filters.py                      # A股5层过滤
│   ├── trend_engine.py                 # 趋势策略引擎v2.0
│   └── scoring.py                      # 综合评分
├── backtest/
│   ├── engine.py                       # 回测引擎(T+1+手续费)
│   └── metrics.py                      # 绩效(夏普/PF/回撤)
├── ai/
│   ├── modelscope_client.py           # ModelScope API
│   └── sentiment.py                    # 情绪增强(可选)
├── notification/
│   └── feishu.py                       # 飞书Bot推送
├── tracker/
│   └── predictor.py                    # 模拟盘追踪+统计
├── app.py                              # Streamlit面板
├── runner.py                           # 主入口
└── reports/                            # 输出JSON
```

## 策略引擎 v2.0

```
输入: 单支股票 250天日K
         │
         ▼
┌─────────────────────────┐
│ 过滤 (4项全过)           │
│ ① Supertrend 多头       │
│ ② Close > MA20          │
│ ③ 40 < RSI < 65         │
│ ④ 量比 ≥ 1.2            │
└───────────┬─────────────┘
            │ 通过
            ▼
┌─────────────────────────┐
│ 入场信号 (≥1个)          │
│ · 突破20日高点           │
│ · MACD金叉 + RSI>50      │
└───────────┬─────────────┘
            │ 触发
            ▼
┌─────────────────────────┐
│ 止损止盈                  │
│ 初始止损 = max(ATR×2.5,  │
│    入场价×6%, MA60,      │
│    10日低点)              │
│ 移动止损 = 最高价-ATR×3  │
│ 最大持仓20天              │
└───────────┬─────────────┘
            │
            ▼
        评分排序 → Top5推荐
```

## 回测规则

| 规则 | 设定 |
|------|------|
| 信号→执行 | T日收盘信号 → **T+1日开盘**入场 |
| 手续费 | 双边 **0.3%** (佣金+印花税+滑点) |
| 涨停过滤 | 次日涨停无法买入 |
| 绩效指标 | 胜率/PF/年化/最大回撤/夏普/按年统计 |

## 回测成绩

| 区间 | 胜率 | PF | 年化 | 最大回撤 |
|------|------|-----|------|------|
| 2023-2026 (10支) | 38.7% | 1.38 | +18.5% | -33.9% |
| 2025-2026 (49支) | 25.0% | 0.89 | -31.5% | -45.2% |

> ⚠️ 2025年后策略失效，当前为**模拟盘观察模式**，不实盘。

## 安全门机制

```
每日推送 → 模拟盘追踪 → 积累虚拟交易 → 月度回测
                                              │
                              近6月回测 > 0?  │
                              ├─ 否 → 继续模拟  │
                              └─ 是 → 切换实盘  │
```

## 运行命令

```bash
# 实时推送（每日）
python runner.py

# 历史回测
python runner.py --backtest --start 2024-01-01 --end 2026-07-01 --stocks 50

# 可视化面板
streamlit run app.py
```
